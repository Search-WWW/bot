'use strict';

class Queue {
    #queue : Array<any> = [];

    constructor() {
        console.log(this.#queue);
    }

    add(elem : any) : number {
        let exists : boolean = false;
        for (let )
    }
}
